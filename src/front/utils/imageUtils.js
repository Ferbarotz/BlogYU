// src/front/utils/imageUtils.js
//export const API_BASE = "https://tu-dominio"; // Cambia por la URL base de tu backend

//export function cleanImageUrl(url) {
  //if (!url) return null;
  //if (/^https?:\/\//i.test(url)) return url;
  //if (url.startsWith("/")) return `${API_BASE.replace(/\/$/, "")}${url}`;
  //if (url.includes("blob") || url.includes("imgur.com")) return null;
 // return `${API_BASE.replace(/\/$/, "")}/${url}`;
//}